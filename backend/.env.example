# ── Required ───────────────────────────────────────────────────────────────────
# MongoDB Atlas connection string (get from atlas.mongodb.com)
MONGODB_URI=mongodb+srv://AMAN:BqDC9zOcPRy77jx2@cluster0.ujesztr.mongodb.net/?appName=Cluster0
# JWT - generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=replace_with_64_char_random_hex_string
JWT_EXPIRE=7d

# ── Optional ───────────────────────────────────────────────────────────────────
PORT=5000
NODE_ENV=production

# Comma-separated list of allowed frontend origins (for CORS)
# Leave blank if frontend is on same domain via Vercel rewrites
ALLOWED_ORIGINS=https://your-app.vercel.app
